<!-- いいね機能ボタン -->
<div id="like-container-<%= post.id %>" class="like-container" data-controller="like">
  <% if user_signed_in? && post.user_id != current_user.id %>
    <div class="flex items-center gap-2">
      <% is_liked = post.liked_by?(current_user) %>
      <!-- いいねボタン -->
      <form id="like-form-<%= post.id %>" 
            action="<%= post_likes_path(post) %>" 
            method="post" 
            style="display: inline;"
            data-like-target="form"
            data-action="submit->like#handleSubmit">
        <input type="hidden" name="_method" value="post">
        <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
        <button type="submit" 
                id="like-btn-<%= post.id %>"
                class="like-button <%= is_liked ? 'liked' : '' %>"
                title="<%= is_liked ? 'いいねを取り消す' : 'いいねする' %>"
                data-like-target="button">
          <span class="text-lg"><%= is_liked ? '❤️' : '🤍' %></span>
        </button>
      </form>

      <!-- いいね数 -->
      <span class="like-count" id="like-count-<%= post.id %>" data-like-target="count">
        <%= post.likes_count %>
      </span>
    </div>
  <% else %>
    <!-- ログインしていない場合 -->
    <div class="flex items-center gap-2">
      <span class="text-lg text-gray-300">🤍</span>
      <span class="like-count">
        <%= post.likes_count %>
      </span>
    </div>
  <% end %>
</div>
